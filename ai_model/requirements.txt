# Phoenix Eduplan v4.0 — requirements.txt
# ── LLM backend (Groq replaces HF Serverless — free & fast) ──
groq>=0.9.0

# ── RAG stack ────────────────────────────────────────────────
langchain>=0.2.0
langchain-community>=0.2.0
langchain-huggingface>=0.0.3
langchain-text-splitters>=0.2.0
faiss-cpu>=1.8.0
sentence-transformers>=2.7.0
rank_bm25>=0.2.2

# ── File loaders ─────────────────────────────────────────────
pypdf>=3.0.0
pdfplumber>=0.11.0
docx2txt>=0.8
python-docx>=1.1.0
python-pptx>=0.6.23
pandas>=2.0.0
openpyxl>=3.1.0
unstructured>=0.14.0

# ── OCR (optional, enable if you need scanned PDFs) ──────────
# pytesseract>=0.3.10
# pdf2image>=1.17.0
Pillow>=10.0.0

# ── Audio / TTS ───────────────────────────────────────────────
gTTS>=2.5.0
# azure-cognitiveservices-speech>=1.38.0  # optional Azure TTS

# ── Video ─────────────────────────────────────────────────────
moviepy==1.0.3
decorator<5.0

# ── API / Web ─────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn>=0.30.0
python-multipart>=0.0.9   # FastAPI file uploads
slowapi>=0.1.9            # rate limiting

# ── HuggingFace fallback (optional, remove if only using Groq) ─
# huggingface-hub>=0.23.0

# ── UI ────────────────────────────────────────────────────────
gradio>=4.40.0
